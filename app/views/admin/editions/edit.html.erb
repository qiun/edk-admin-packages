<% content_for :title, "Edycja: #{@edition.name}" %>
<% content_for :breadcrumb, "Edytuj edycję" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Edytuj edycję</h1>
    <p class="text-gray-600 dark:text-gray-400">Aktualizuj dane edycji <%= @edition.name %></p>
  </div>

  <%= render "form" %>
</div>
