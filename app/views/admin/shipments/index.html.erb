<% content_for :title, "Wysyłki" %>
<% content_for :breadcrumb, "Wysyłki" %>

<div class="space-y-6">
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
    <div>
      <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Wysyłki</h1>
      <p class="text-gray-600 dark:text-gray-400">Zarządzaj wysyłkami pakietów przez aPaczka.pl</p>
    </div>
  </div>

  <!-- Filters -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 shadow-sm">
    <div class="flex flex-wrap gap-2">
      <%= link_to "Wszystkie", admin_shipments_path,
          class: "px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{params[:status].blank? ? 'bg-brand-500 text-white' : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300'}" %>
      <%= link_to "Oczekujące", admin_shipments_path(status: :pending),
          class: "px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{params[:status] == 'pending' ? 'bg-yellow-500 text-white' : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300'}" %>
      <%= link_to "Etykieta wydrukowana", admin_shipments_path(status: :label_printed),
          class: "px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{params[:status] == 'label_printed' ? 'bg-blue-500 text-white' : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300'}" %>
      <%= link_to "Wysłane", admin_shipments_path(status: :shipped),
          class: "px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{params[:status] == 'shipped' ? 'bg-purple-500 text-white' : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300'}" %>
      <%= link_to "W drodze", admin_shipments_path(status: :in_transit),
          class: "px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{params[:status] == 'in_transit' ? 'bg-indigo-500 text-white' : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300'}" %>
      <%= link_to "Dostarczone", admin_shipments_path(status: :delivered),
          class: "px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{params[:status] == 'delivered' ? 'bg-green-500 text-white' : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300'}" %>
      <%= link_to "Nieudane", admin_shipments_path(status: :failed),
          class: "px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{params[:status] == 'failed' ? 'bg-red-500 text-white' : 'bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300'}" %>
    </div>
  </div>

  <!-- Shipments Table -->
  <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm overflow-hidden">
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
        <thead class="bg-gray-50 dark:bg-gray-700">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-300 uppercase">#</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-300 uppercase">Źródło</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-300 uppercase">Nr listu</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-300 uppercase">Status</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-300 uppercase">Data</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 dark:text-gray-300 uppercase">Akcje</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200 dark:divide-gray-700">
          <% @shipments.each do |shipment| %>
            <tr class="hover:bg-gray-50 dark:hover:bg-gray-700/50">
              <td class="px-6 py-4 text-sm font-medium text-gray-900 dark:text-white">#<%= shipment.id %></td>
              <td class="px-6 py-4">
                <% if shipment.order.present? %>
                  <div>
                    <p class="text-sm font-medium text-gray-900 dark:text-white">
                      <%= link_to "Zamówienie ##{shipment.order.id}", admin_order_path(shipment.order), class: "text-brand-500 hover:text-brand-600" %>
                    </p>
                    <p class="text-xs text-gray-500 dark:text-gray-400"><%= shipment.order.user.full_name %></p>
                  </div>
                <% elsif shipment.donation.present? %>
                  <div>
                    <p class="text-sm font-medium text-gray-900 dark:text-white">Cegiełka #<%= shipment.donation.id %></p>
                    <p class="text-xs text-gray-500 dark:text-gray-400"><%= shipment.donation.donor_name %></p>
                  </div>
                <% end %>
              </td>
              <td class="px-6 py-4">
                <% if shipment.waybill_number.present? %>
                  <span class="text-sm font-mono text-gray-900 dark:text-white"><%= shipment.waybill_number %></span>
                <% else %>
                  <span class="text-sm text-gray-400 dark:text-gray-500">—</span>
                <% end %>
              </td>
              <td class="px-6 py-4"><%= status_badge(shipment.status) %></td>
              <td class="px-6 py-4 text-sm text-gray-500 dark:text-gray-400"><%= shipment.created_at.strftime("%d.%m.%Y") %></td>
              <td class="px-6 py-4 text-right text-sm font-medium space-x-2">
                <%= link_to "Zobacz", admin_shipment_path(shipment), class: "text-brand-500 hover:text-brand-600" %>
                <% if shipment.tracking_url.present? %>
                  <%= link_to "Śledź", shipment.tracking_url, target: "_blank", class: "text-purple-600 hover:text-purple-700" %>
                <% end %>
              </td>
            </tr>
          <% end %>
          <% if @shipments.empty? %>
            <tr>
              <td colspan="6" class="px-6 py-12 text-center text-gray-500 dark:text-gray-400">
                Brak wysyłek
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
