<% content_for :page_title, "Dziękujemy za wsparcie!" %>

<div class="min-h-screen bg-gradient-to-b from-green-50 to-white dark:from-gray-900 dark:to-gray-800 flex items-center justify-center px-4 py-12">
  <div class="max-w-lg w-full text-center">
    <div class="mb-8">
      <div class="mx-auto w-20 h-20 bg-green-100 dark:bg-green-900 rounded-full flex items-center justify-center">
        <svg class="w-10 h-10 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
      </div>
    </div>
    
    <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-4">
      Dziękujemy za Twoje wsparcie!
    </h1>
    
    <p class="text-lg text-gray-600 dark:text-gray-300 mb-8">
      Twoja cegiełka została przyjęta. Na podany adres email wyślemy potwierdzenie płatności.
    </p>
    
    <% if @donation&.want_gift %>
      <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-6 mb-8 text-left">
        <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">📦 Szczegóły wysyłki</h2>
        <div class="space-y-2 text-sm text-gray-600 dark:text-gray-300">
          <p><strong>Ilość pakietów:</strong> <%= @donation.quantity %></p>
          <p><strong>Paczkomat:</strong> <%= @donation.locker_code %> - <%= @donation.locker_name %></p>
          <p><strong>Adres:</strong> <%= @donation.locker_city %>, <%= @donation.locker_post_code %></p>
        </div>
        <p class="mt-4 text-sm text-gray-500 dark:text-gray-400">
          Otrzymasz powiadomienie SMS, gdy paczka będzie gotowa do odbioru.
        </p>
      </div>
    <% end %>
    
    <a href="https://edk.org.pl" 
       class="inline-flex items-center justify-center px-6 py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-medium rounded-lg transition">
      Wróć na stronę EDK
    </a>
  </div>
</div>

<%= render "public/donations/footer" %>
